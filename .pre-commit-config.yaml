repos:
  - repo: local
    hooks:
      - id: check-large-files
        name: Check large files (unstage >10MB)
        entry: scripts/check-large-files.sh
        language: script
        stages: [pre-commit]
